spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/ya_market_test
    username: test
    password: test

  liquibase:
    # db connection
    url: jdbc:postgresql://localhost:5433/ya_market
    user: ${spring.r2dbc.username}
    password: ${spring.r2dbc.password}
    # main settings
    change-log: classpath:db/changelog/db.changelog-master-test.xml
    enabled: true
